# messaging-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
    name: messaging-service
spec:
    replicas: 1
    selector:
        matchLabels:
            app: messaging-service
    template:
        metadata:
            labels:
                app: messaging-service
        spec:
            containers:
                - name: messaging-service
                  image: skillbridge-messaging-service
                  imagePullPolicy: Never
                  ports:
                      - containerPort: 3002
                  envFrom:
                      - configMapRef:
                            name: messaging-env
---
apiVersion: v1
kind: Service
metadata:
    name: messaging-service
spec:
    type: ClusterIP
    selector:
        app: messaging-service
    ports:
        - port: 3002
          targetPort: 3002
